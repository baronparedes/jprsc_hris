@model JPRSC.HRIS.WebApp.Features.ApprovalLevels.Edit.Command

@{
    ViewBag.Title = "Edit Approval Level " + Convert.ToString(Model.Level);
}

<div class="page-content-wrapper">
    <div class="page-content">
        <div class="page-bar">
            <ul class="page-breadcrumb">
                <li>
                    <a href="@Url.Action("Index", "Home")">Home</a>
                    <i class="fa fa-circle"></i>
                </li>
                <li>Edit Approval Level</li>
            </ul>
        </div>
        <h1 class="page-title">
            Edit Approval Level @Model.Level
        </h1>
        <div class="row">
            <div class="col-sm-12">
                <div class="portlet light bordered">
                    <div class="portlet-title">
                        <div class="caption font-red-sunglo">
                            <i class="fa fa-check font-red-sunglo"></i>
                            <span class="caption-subject font-red-sunglo bold uppercase">Edit Approval Level @Model.Level</span>
                        </div>
                    </div>
                    <div class="portlet-body form" ng-controller="EditApprovalLevelCtrl as vm">
                        <form class="form-horizontal" role="form" method="post" ng-submit="vm.editApprovalLevelSubmit($event)">
                            @Html.AntiForgeryToken()
                            @Html.HiddenFor(m => m.Id)
                            <div class="form-body">
                                @Html.TextBoxHorizontalFormGroup("Level")
                                @Html.DropdownHorizontalFormGroup(Model.UsersList, "UserId", "Approver")
                            </div>
                            <div class="form-actions right">
                                <a href="@Url.Action("Index", "ApprovalLevels")" class="btn default">Cancel</a>
                                <button type="submit" class="btn green">Save</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section scripts
{
    @Scripts.Render("~/scriptbundles/approvalLevels")
}