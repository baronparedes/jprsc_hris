@model JPRSC.HRIS.WebApp.Features.Accounts.Add.Command

@{
    ViewBag.Title = "Add a Account";
}

<div class="page-content-wrapper">
    <div class="page-content">
        <div class="page-bar">
            <ul class="page-breadcrumb">
                <li>
                    <a href="@Url.Action("Index", "Home")">Home</a>
                    <i class="fa fa-circle"></i>
                </li>
                <li>Add Account</li>
            </ul>
        </div>
        <h1 class="page-title">
            Add Account
        </h1>
        <div class="row">
            <div class="col-sm-12">
                <div class="portlet light bordered">
                    <div class="portlet-title">
                        <div class="caption font-red-sunglo">
                            <i class="fa fa-plus font-red-sunglo"></i>
                            <span class="caption-subject font-red-sunglo bold uppercase">Add Account</span>
                        </div>
                    </div>
                    <div class="portlet-body form" ng-controller="AddAccountCtrl as vm">
                        <form class="form-horizontal" role="form" method="post" ng-submit="vm.addAccountSubmit($event)">
                            @Html.AntiForgeryToken()
                            <div class="form-body">
                                @Html.TextBoxHorizontalFormGroup("Name")
                                @Html.TextBoxHorizontalFormGroup("JobTitle")
                                @Html.TextBoxHorizontalFormGroup("UserName")
                                @Html.PasswordBoxHorizontalFormGroup("Password")
                                @Html.PasswordBoxHorizontalFormGroup("RepeatPassword")
                                <div class="form-group">
                                    <label class="col-md-3 control-label">Roles</label>
                                    <div class="col-md-9">
                                        <div class="mt-checkbox-list">
                                            @for (var i = 0; i < Model.RolesList.Count; i++)
                                            {
                                                <input type="hidden" name="@String.Format("RolesList[{0}].Value", i)" value="@Model.RolesList[i].Value" />
                                                <input type="hidden" name="@String.Format("RolesList[{0}].Text", i)" value="@Model.RolesList[i].Text" />
                                                <label class="mt-checkbox mt-checkbox-outline">
                                                    <input type="checkbox" name="@String.Format("RolesList[{0}].Selected", i)" checked="@Model.RolesList[i].Selected" value="true"> @Model.RolesList[i].Text
                                                    <span></span>
                                                </label>
                                            }
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="form-actions right">
                                <a href="@Url.Action("Index", "Accounts")" class="btn default">Cancel</a>
                                <button type="submit" class="btn green">Add</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section scripts
{
    @Scripts.Render("~/scriptbundles/accounts")    
}